# Docker Compose AWS 연결용 설정
# 로컬 개발시 .env.dev를 생성하고 AWS 정보를 입력하세요

##########################
# 네트워크 (자동 생성 가능)
##########################
networks:
  wordle-net:

##########################
# 볼륨 (AWS 연결용)
##########################
volumes:
  gradle_cache:           # Gradle ~/.gradle 캐시
  postgres_local_data:    # 로컬 DB용 (선택사항)

##########################
# 서비스 정의 (AWS 연결)
##########################
services:
  # ---------- ① Spring Backend (AWS RDS 연결) ----------
  backend:
    build:
      context: ./backend         # backend/ 디렉토리의 Dockerfile 사용
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      # AWS 연결 환경변수 (환경별 .env 파일에서 로드)
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-dev}
      TZ: Asia/Seoul
      
      # AWS RDS 연결 정보 (환경별로 다른 변수 사용)
      # DEV 환경
      AWS_DEV_DB_URL: ${AWS_DEV_DB_URL:-}
      AWS_DEV_DB_USERNAME: ${AWS_DEV_DB_USERNAME:-}
      AWS_DEV_DB_PASSWORD: ${AWS_DEV_DB_PASSWORD:-}
      
      # TEST 환경 (DEV와 같은 서버, 다른 DB)
      AWS_TEST_DB_URL: ${AWS_TEST_DB_URL:-}
      AWS_TEST_DB_USERNAME: ${AWS_TEST_DB_USERNAME:-}
      AWS_TEST_DB_PASSWORD: ${AWS_TEST_DB_PASSWORD:-}
      
      # PROD 환경 (별도 서버)
      AWS_PROD_DB_URL: ${AWS_PROD_DB_URL:-}
      AWS_PROD_DB_USERNAME: ${AWS_PROD_DB_USERNAME:-}
      AWS_PROD_DB_PASSWORD: ${AWS_PROD_DB_PASSWORD:-}
      
      # OAuth2 JWT 키스토어 (RSA)
      JWT_KEYSTORE_LOCATION: ${JWT_KEYSTORE_LOCATION:-classpath:keystore.p12}
      JWT_KEYSTORE_PASSWORD: ${JWT_KEYSTORE_PASSWORD:-changeit}
      JWT_KEYSTORE_ALIAS: ${JWT_KEYSTORE_ALIAS:-wordle-jwt}

      # entrypoint.sh 스크립트용 키스토어 자동 생성 설정
      KEYSTORE_PATH: ${KEYSTORE_PATH:-/workspace/src/main/resources/keys/keystore.p12}
      KEY_ALIAS: ${KEY_ALIAS:-oauth-key}
      KEYSTORE_PW: ${KEYSTORE_PW:-changeit}
      DNAME: ${DNAME:-CN=wordle-auth, OU=Dev, O=Wordle, L=Seoul, S=Seoul, C=KR}

      # API JWT 시크릿 (HMAC-SHA256) — 반드시 .env 파일에서 오버라이드
      JWT_SECRET: ${JWT_SECRET:-cosmic-wordle-default-secret-key-change-in-production}
      JWT_EXPIRATION_MS: ${JWT_EXPIRATION_MS:-86400000}

      # CORS 허용 출처 — 프로덕션에서는 실제 도메인으로 변경
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS:-http://localhost:3000}

      # 로깅 레벨 — 개발: INFO/DEBUG, 프로덕션: WARN
      LOGGING_LEVEL_ROOT: ${LOGGING_LEVEL_ROOT:-INFO}

      # Gradle 캐시
      GRADLE_USER_HOME: /home/gradle/.gradle

      # AWS 리전
      AWS_REGION: ${AWS_REGION:-ap-northeast-2}
      
    volumes:
      # 코드 동기화 (핫 리로드)
      - ./backend:/workspace
      # Gradle 캐시 공유로 빌드 속도 향상
      - gradle_cache:/home/gradle/.gradle
      # entrypoint 스크립트 마운트
      - ./backend/docker/entrypoint.sh:/workspace/docker/entrypoint.sh:ro
      
    healthcheck:                 # Actuator 사용 (8080/actuator/health)
      test: ["CMD-SHELL", "curl -f http://localhost:8080/actuator/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s          # AWS 연결 시간 고려하여 증가
      
    networks:
      - wordle-net
    ports:
      - "${SERVER_PORT:-8080}:8080"
    entrypoint: ["sh", "/workspace/docker/entrypoint.sh"]  # Dockerfile ENTRYPOINT 사용

  # ---------- ② Next.js Frontend ----------
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:8080}
      PORT: 3000
    ports:
      - "${FRONTEND_PORT:-3000}:3000"
    networks:
      - wordle-net
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3000 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
