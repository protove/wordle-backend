# 개발 환경 변수 템플릿
# 실제 사용시 .env.dev로 복사하여 값을 채워주세요

# AWS RDS 개발 서버 설정
AWS_DEV_DB_URL=DB_URL
AWS_DEV_DB_USERNAME=DB_USERNAME
AWS_DEV_DB_PASSWORD=DB_PASSWORD

# JWT 키스토어 설정
JWT_KEYSTORE_LOCATION=classpath:keys/keystore.p12
JWT_KEYSTORE_PASSWORD=changeit
JWT_KEYSTORE_ALIAS=oauth-key

# entrypoint.sh 스크립트용 키스토어 설정
KEYSTORE_PATH=/workspace/src/main/resources/keys/keystore.p12
KEY_ALIAS=oauth-key
KEYSTORE_PW=your_secure_keystore_password
DNAME=CN=wordle-auth, OU=Dev, O=Wordle, L=Seoul, S=Seoul, C=KR

# AWS 리전 설정
AWS_REGION=ap-northeast-2

# 개발 환경 설정
SPRING_PROFILES_ACTIVE=dev
SERVER_PORT=8080

# 선택적 설정
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001
LOGGING_LEVEL_ROOT=INFO

# JWT 설정 (심플 HMAC-SHA256)
JWT_SECRET=your-secret-key-min-32-characters-long-change-in-production
JWT_EXPIRATION_MS=86400000

# ───────────────────────────────────────────
# Frontend (Next.js) 설정
# ───────────────────────────────────────────
# 브라우저에서 백엔드 API를 호출할 주소 (공개 접근 가능한 URL)
NEXT_PUBLIC_API_URL=http://localhost:8080
# 프론트엔드 포트 (기본값: 3000)
FRONTEND_PORT=3000
